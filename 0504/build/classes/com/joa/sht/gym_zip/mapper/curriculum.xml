<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="curriculum">
	<!--0127 백경현 -->
	<insert id='insertCurriculum' parameterType="Curriculum">
		INSERT INTO curriculum(no, exercise_no, lecture_no, seq, regdate)
		VALUES(curriculum_seq.nextval,#{exerciseNo},#{lectureNo},#{seq},sysdate)
	</insert>

</mapper>